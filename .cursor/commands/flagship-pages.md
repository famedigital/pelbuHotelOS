# Build / harden flagship public pages

Use for Pelbu Suites `template_id: 1` public site.

1. Read `AGENTS.md`, `docs/PLATFORM.md` design section, `design/mockups/`
2. Follow pelbu-design-system + production-quality rules
3. Implement real pages (Home, Rooms, Cafe, Restaurant, Bar, Spa, Meeting, Book, Order, Agents, Contact)
4. Use Cloudinary CDN URLs for images (`pelbu/...` folders)
5. No scaffold/placeholder copy on public routes
6. Run `npm run build` in `web/` before finishing
