# Implement: rooms + cafe page polish (GLM)

You are the **implementer**. Do **not** rewrite this brief. **Edit the files now.**

## Task
Polish `/rooms` and `/cafe` copy and layout. Keep data fetching and CTAs.

## Allowed files only
- `web/src/app/rooms/page.tsx` (copy/layout only — keep Supabase queries)
- `web/src/app/cafe/page.tsx` (copy/layout only — keep Supabase queries)

## Do not touch
- `web/src/app/erp/**`
- `web/src/app/actions/**`
- `supabase/**`
- Do not invent room rates in Nu

## When done
Write file changes; reply with what changed / how to test.
