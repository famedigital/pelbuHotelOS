/** Default demo / seed property slug when no host or flagship mapping applies. */
export const DEFAULT_PROPERTY_SLUG = "demo-hotel" as const;

/** @deprecated Use DEFAULT_PROPERTY_SLUG */
export const PELBU_PROPERTY_SLUG = DEFAULT_PROPERTY_SLUG;
